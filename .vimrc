syntax on
colorscheme torte
set number
set relativenumber
set nowrap 
set autoindent
set smartindent
set autoindent
set sidescroll=1     " Scroll sideways one column at a time
set sidescrolloff=5  " Keep 5 columns to the left and right of the cursor  
set tabstop=4        " Set the width of a tab character to 4 spaces
set shiftwidth=4     " Set the number of spaces to use for each step of (auto)indent
set expandtab        " Convert tabs to spaces
set ignorecase       " Ignore case in search patterns
set incsearch        " Show partial matches while typing search patterns
set scrolloff=2      " Keep 2 lines above and below the cursor when scrolling vertically
set hlsearch         " Highlight search results
